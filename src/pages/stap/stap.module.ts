import { NgModule } from '@angular/core';
import { IonicPageModule } from 'ionic-angular';
import { StapPage } from './stap';

@NgModule({
  declarations: [
    StapPage,
  ],
  imports: [
    IonicPageModule.forChild(StapPage),
  ],
})
export class StapPageModule {}
